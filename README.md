HangMan
A command line Hangman game written in Python.

HangMan uses the wonderwords library to select a random word for users to guess. It tracks the user's score and prints out their guessed letters, while also drawing the 'hangman' in lines and slashes.
